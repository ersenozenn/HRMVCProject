@model CostAndTypesVM

@{
    ViewData["Title"] = "Harcama-Talebi";  
}

<header class="page-header">
	<h2>Harcama Talebi</h2>
	<div class="right-wrapper pull-right">
		<ol class="breadcrumbs">
		@*	<li>
				<a asp-action="Index" asp-controller="Employee">
					<i class="fa fa-home"></i>
				</a>
			</li>*@
			<li><span>Harcama</span></li>
			<li><span>Harcama Talebi</span></li>
		</ol>				
    </div>
</header>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CostCreate" enctype="multipart/form-data" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Cost.Amount" class="control-label"></label>
                <input asp-for="Cost.Amount" class="form-control" />
                <span asp-validation-for="Cost.Amount" class="text-danger"></span>
            </div>
            @* harcama türü seçiyor*@
        @*    <div class="form-group">
                <label asp-for="CostType.CostName" class="control-label"></label>
                <select asp-for="CostTypeId" class="form-control" asp-items="@(new SelectList(ViewBag.CostTypes,"CostTypeId","CostName"))"></select>
            </div>*@
              <div class="form-group">
                <label asp-for="Cost.CostTypeId" class="control-label"></label>
                <select asp-for="Cost.CostTypeId" class ="form-control" asp-items="ViewBag.PermissionTypeID">
                    @{
                        foreach (var item in Model.CostTypes)
                        {
                            <option value="@item.Id">@item.CostName</option>
                        }
                    }
                </select>
            </div>
            <div class="form-group">
                <label asp-for="Cost.CostFile" class="control-label"></label>            
                 <input type="file" asp-for="Cost.CostFile" class="form-control" accept="image/jpeg,image/png/vnd.ms-excel, application/vnd.ms-powerpoint,
text/plain, application/pdf, image/*" />
                <span asp-validation-for="Cost.CostFile" class="text-danger"></span>
            </div>
            	<div class="text-danger" asp-validation-summary="ModelOnly"> </div>
            <div class="form-group">
                <input type="submit" value="Oluştur" class="btn btn-default"asp-route-id="@Model.EmployeeId" />
            </div>
        </form>
    </div>
</div>

